# Example crontab entries for local data refresh
# Install with: crontab -e
# Replace /path/to/repo/defi-analytica with your local checkout path.
#
# Every 15 minutes, run local refresh against the default dev server.
*/15 * * * * cd /path/to/repo/defi-analytica && ./scripts/cron-refresh-local.sh >> /tmp/defi-analytica-refresh.log 2>&1

# Dry-run every hour for diagnostics (optional).
# 0 * * * * cd /path/to/repo/defi-analytica && REFRESH_DRY_RUN=true ./scripts/cron-refresh-local.sh >> /tmp/defi-analytica-refresh-dry.log 2>&1
